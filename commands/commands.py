from commands.commands_src import *
commands = {"time": time,
            "echo": echo,
            "clear": clear,
            "c2": lambda x: pass,
            "info": lambda x: pass,
            "show": lambda x: pass,
            "console": lambda x: pass,
            "upload": lambda x: pass,
            "help": lambda x: pass,
            "hello": hello}
